# Copy to .env.local for local dev. These end up in the client bundle and are public
# by design — protect the data with RLS policies, not by hiding the anon key.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
